# Travis CI Demo 🚀

Projet de démonstration pour l'exposé Travis CI.

## Structure

- `index.js` — 3 fonctions mathématiques simples
- `index.test.js` — Tests Jest pour chaque fonction
- `.travis.yml` — Configuration Travis CI

## Lancer les tests localement

```bash
npm install
npm test
```
